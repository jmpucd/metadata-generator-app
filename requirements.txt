# Core web UI
streamlit>=1.35.0

# CLI
typer[all]>=0.12.0

# Database
sqlalchemy>=2.0.0

# Image handling
Pillow>=10.0.0

# Data export
pandas>=2.0.0

# XMP/IPTC export (requires ExifTool installed on the system)
pyexiftool>=0.5.6

# Local VLM — Qwen-VL via transformers
# Comment these out if you're using Ollama or another backend instead
transformers>=4.40.0
accelerate>=0.30.0
torch>=2.2.0
# For Qwen2-VL specifically:
# qwen-vl-utils>=0.0.2

# Dagster
dagster>=1.7.0
dagster-webserver>=1.7.0

# Utilities
python-dotenv>=1.0.0
rich>=13.0.0          # Pretty CLI output (used by Typer)
